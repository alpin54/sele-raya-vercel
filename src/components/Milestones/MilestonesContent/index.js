/* ------------------------------------------------------------------------------
@name: MilestonesContent
@description: MilestonesContent
--------------------------------------------------------------------------------- */

const MilestonesContent = (() => {
  const handleMilestonesContent = () => {};

  // Init
  const init = () => {
    handleMilestonesContent();
  };

  return { init };
})();

export default MilestonesContent;
